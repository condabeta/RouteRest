Fix missing static assets and remove Loom script

- Add the referenced but missing public assets so nothing 404s at runtime:
  truck.svg (favicon, referenced by index.html) and hero-truck-default.svg
  (the fallback layer beneath every hero slide). These previously triggered
  Vite "didn't resolve at build time" warnings and 404s in the browser.
- Drop the dead url("/driver1.jpg") reference from the unused .driver-card
  rule so the stylesheet has no broken image references.
- Remove LOOM_SCRIPT.md (personal recording notes, not part of the deliverable).

Build is now warning-free.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
